import { EfetivacaoPipe } from './efetivacao.pipe';

describe('EfetivacaoPipe', () => {
  it('create an instance', () => {
    const pipe = new EfetivacaoPipe();
    expect(pipe).toBeTruthy();
  });
});
